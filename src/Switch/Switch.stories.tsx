import * as React from 'react';
import { Switch } from '.';
import { Box } from '../Core';

export default {
  title: 'Switch',
};

export const switchComponent = () => (
  <Box>
    <Switch />
  </Box>
);
