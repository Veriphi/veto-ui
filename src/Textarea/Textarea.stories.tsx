import * as React from 'react';
import { Textarea } from '.';

export default {
  title: 'Textarea ',
};

export const textarea = () => (
  <div>
    <h2>Textarea Element</h2>
    <Textarea />
  </div>
);
